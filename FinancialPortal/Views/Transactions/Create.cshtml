@using FinancialPortal.Models
@model Group
<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0 text-dark" style="font-weight: bold; font-size: 300%">Create Transaction</h1>

            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->
<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        @*@using (Html.BeginForm("Create", "Tickets", FormMethod.Post, new { id = "ticketForm" }))
        {
            @Html.AntiForgeryToken()
            @Html.Hidden("priorityName", null, new { id = "hiddenTypeId" })
            @Html.Hidden("typeName", null, new { id = "hiddenAccountId" })
            @Html.Hidden("userId", Model.User.Id)
            @Html.Hidden("Ticket.ProjectId", Model.Ticket.ProjectId)
            @Html.HiddenFor(m => m.Project.Id, new { id = "hiddenProjectId" })
            @Html.HiddenFor(m => m.Ticket.AssignedToUserId, new { id = "hiddenDeveloperId" })*@

            <div class="row">
                <div class="col-3">
                    <div class="accordion" id="ticketAccordion">

                        <div id="budgetHeader" class="info-box mb-3" style="cursor: pointer" data-target="#budgetCollapse" data-toggle="collapse" aria-expanded="true" aria-controls="budgetCollapse">
                            <span id="spanBudgetBgColor" class="info-box-icon bg-gradient-cyan elevation-1"><i id="spanBudgetIcon" class="fas fa-exclamation"></i></span>
                            <div class="info-box-content">
                                <span class="info-box-text" style="font-size: large; font-weight: bold">Budget:</span>
                                <span id="spanBudget" class="info-box-text">Select a Budget</span>
                            </div>
                            <i class="fas fa-chevron-down float-right"></i>
                            <!-- /.info-box-content -->
                        </div>
                        <div id="budgetCollapse" class="collapse" aria-labelledby="budgetHeader"
                             data-parent="#ticketAccordion">
                            @foreach (var budget in Model.Budgets)
                            {
                                <div style="padding-left: 20%">
                                    <a class="aBudgetClass" href="#" style="color:black">
                                        <div class="info-box mb-3" style="cursor: pointer" data-target="#budgetCollapse" data-toggle="collapse" aria-expanded="true" aria-controls="roleCollapse">
                                            <span class="info-box-icon bg-gradient-green elevation-1"><i class="iBudgetClass fas fa-layer-group"></i></span>
                                            <div class="info-box-content">
                                                <span class="info-box-text" style="font-size: large; font-weight: bold">@budget.Name</span>
                                            </div>
                                            <!-- /.info-box-content -->
                                        </div>
                                    </a>
                                </div>
                            }

                        </div>



                        <div>
                            <h3 id="errorHeader" style="font-weight: bold; text-align: center"></h3>
                        </div>

                        <div>
                            <a href=@Url.Action("Index", "Home") style="color: black">
                                <div class="info-box mb-3 bg-red">
                                    <span class="info-box-icon bg-white elevation-1"><i class="fas fa-exclamation-triangle" style="color: #dc3545"></i></span>
                                    <div class="info-box-content">
                                        <span class="info-box-text" style="font-size: large; font-weight: bold">Cancel</span>
                                    </div>
                                    <!-- /.info-box-content -->
                                </div>
                            </a>
                        </div>
                        <div>
                            <a id="submitTransactionId" href="#" onclick="submitTransaction()">
                                <div class="info-box mb-3 bg-green">
                                    <span class="info-box-icon bg-white elevation-1"><i class="fas fa-check" style="color: #208637"></i></span>
                                    <div class="info-box-content">
                                        <span class="info-box-text" style="font-size: large; font-weight: bold">Submit</span>
                                    </div>
                                    <!-- /.info-box-content -->
                                </div>
                            </a>
                        </div>
                    </div>
                    

                </div>

                <div class="col">
                    <div class="card card-widget">
                        <div class="card-header">
                            <div class="user-block">
                                <img class="img-circle" src="../dist/img/user1-128x128.jpg">
                                <span class="username">DISPLAYNAME</span>
                                <span class="description">DATETIMENOW</span>
                            </div>
                            <!-- /.user-block -->
                            <div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse">
                                    <i class="fas fa-minus"></i>
                                </button>
                            </div>
                            <!-- /.card-tools -->
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <h3 style="font-weight: bold">Memo</h3>
                            @*@Html.TextBoxFor(model => model.Ticket.Title, new { id = "ticketTitleId", type = "text", style = "width: 50%" })*@
                            <textarea style="width: 100%" class="form-control" rows="9"></textarea>
                        </div>
                        <!-- /.card-body -->
                    </div>
                </div>
            </div>
        @*}*@
    </div>
</section>






@section scripts {

    <script>
        var accountText = ""
        var typeText = ""
        var budgetText = ""

        $(document).ready(function () {



                @*@if (Model.Project != null)
            {
                @:projectId = @Model.Project.Id
                @:projectName = "@Model.Project.Name"
                @:$("#projectInfoBox").removeClass()
                @:$("#projectBgSpan").removeClass()
                @:$("#projectIcon").removeClass()
                @:$("#projectInfoBox").addClass("info-box mb-3 bg-gradient-blue")
                @:$("#projectBgSpan").addClass("info-box-icon bg-white elevation-1")
                @:$("#projectIcon").addClass("fas fa-check")
                @:$("#projectIcon").css("color", "#0067d6")
                @:$("#projectName").text(projectName)

                @:$("#hiddenProjectId").val(projectId)
            }*@

        })

        $(".aBudgetClass").on("click", function () {
            //Step 1: Get the icon from the thing we clicked
            var icon = $(this).find(".iBudgetClass").attr("class").replace("iBudgetClass", "");

            //Step 2: Get the BG color...
            var bgColor = $(this).find(".elevation-1").attr("class");

            //Step 3: Get the text from the thing we clicked
            budgetText = $(this).find(".info-box-content > span").text();

            //Get the current class we are replacing
            var bgClass = $("#spanBudgetBgColor").attr("class");
            $("#spanBudgetBgColor").removeClass(bgClass);
            $("#spanBudgetBgColor").addClass(bgColor);

            var icoClass = $("#spanBudgetIcon").attr("class");
            $("#spanBudgetIcon").removeClass(icoClass);
            $("#spanBudgetIcon").addClass(icon);
            $("#spanBudget").text(budgetText);
            $("#hiddenBudgetId").val(budgetText);
            $("#errorHeader").text("")
        })

        function submitTransaction() {
            var projectIsSelected = false
            var typeIsSelected = false
            var priorityIsSelected = false
            var titleIsEntered = false
            var descriptionIsEntered = false
            var ticketTitle = $("#ticketTitleId").val()
            var ticketDescription = $("#ticketDescriptionId").val()

            if (projectId != "") {
                projectIsSelected = true
            }
            if (typeText != "") {
                typeIsSelected = true
            }
            if (priorityText != "") {
                priorityIsSelected = true
            }
            if (ticketDescription != "") {
                descriptionIsEntered = true
            }
            if (ticketTitle != "") {
                titleIsEntered = true
            }

            if (priorityIsSelected && typeIsSelected && projectIsSelected && titleIsEntered && descriptionIsEntered) {
                $("#ticketForm").submit();
            } else {
                $("#errorHeader").text("Fill the fields above.")
            }

            if (!descriptionIsEntered) {
                $("#errorHeader").text("Enter a description.")
            }
            if (!titleIsEntered) {
                $("#errorHeader").text("Enter a title.")
            }


        }


    </script>





}`
